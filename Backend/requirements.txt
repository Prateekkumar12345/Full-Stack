# Core requirements
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.11.0
python-dateutil>=2.8.0

# Optional but recommended for better performance
scipy>=1.7.0
joblib>=1.0.0

# For the web interface
gradio>=3.0.0

# For data loading
openpyxl>=3.0.0  # For Excel file support

# For model saving/loading
pickle-mixin>=1.0.0

# Optional sentence transformers (will use TF-IDF fallback if not available)
sentence-transformers>=2.2.0

# For LLM fallback (optional)
groq>=0.3.0